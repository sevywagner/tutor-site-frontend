import mainStyles from './../globalCss/main.module.css';

const Home = () => {
    return (
        <div>
            <p className={mainStyles.title}>Home</p>
        </div>
    );
}

export default Home;